import React from 'react';
import { useParams } from 'react-router-dom';

const Sobre = () => {
  const params = useParams();
  return <div className="mx-auto w-full pl-[300px] relative">Sobre</div>;
};

export default Sobre;
